<Window x:Class="Erronka.Views.AdminMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Erronka.ViewModels"
        Title="Administratzailearen panela" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <!-- DataContext set in code-behind to avoid designer resolution issues -->

    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Width="180" Background="#2E3A59" Margin="10">
            <TextBlock Text="Kudeaketa panela" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,20"/>
            <Button Content="Erabiltzaileak" Command="{Binding ShowUsersCommand}" Margin="0,5"/>
            <Button Content="Stocka" Command="{Binding ShowStockCommand}" Margin="0,5"/>
            <Button Content="Itxi saioa" Command="{Binding LogoutCommand}" Margin="0,20,0,0"/>
        </StackPanel>

        <ContentControl Content="{Binding CurrentView}"/>
    </DockPanel>
</Window>