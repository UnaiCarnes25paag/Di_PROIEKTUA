<UserControl x:Class="Erronka.Views.StockView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Erronka.ViewModels">
    <!-- DataContext set in code-behind -->

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>    <!-- toolbar -->
            <RowDefinition Height="*"/>       <!-- grid -->
            <RowDefinition Height="Auto"/>   <!-- editor -->
            <RowDefinition Height="Auto"/>   <!-- buttons -->
        </Grid.RowDefinitions>

        <!-- Toolbar with New button -->
        <StackPanel Orientation="Horizontal" Grid.Row="0" HorizontalAlignment="Right" Margin="0,0,0,8">
            <Button Content="+" Width="28" Height="28" ToolTip="Nuevo stock" Command="{Binding NewCommand}" />
        </StackPanel>

        <!-- Stock list -->
        <DataGrid Grid.Row="1" ItemsSource="{Binding Stocks}" SelectedItem="{Binding SelectedStock}"
                  AutoGenerateColumns="False" Margin="0,0,0,10" IsReadOnly="True">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Produktua" Binding="{Binding Product.Name}" Width="*"/>
                <DataGridTextColumn Header="Prezioa" Binding="{Binding Product.Price}" Width="100"/>
                <DataGridTextColumn Header="Kopurua" Binding="{Binding Quantity}" Width="80"/>
                <DataGridTextColumn Header="Location" Binding="{Binding Location}" Width="120"/>
            </DataGrid.Columns>
        </DataGrid>

        <!-- Editor bound to SelectedStock -->
        <Grid Grid.Row="2" Margin="0,0,0,10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <StackPanel Grid.Column="0" Orientation="Vertical" VerticalAlignment="Top" >
                <TextBlock Text="Produktua" Margin="0,0,0,4"/>
                <ComboBox ItemsSource="{Binding Products}" DisplayMemberPath="Name" SelectedValuePath="Id"
                          SelectedValue="{Binding SelectedStock.ProductId, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Kopurua" Margin="0,8,0,4"/>
                <TextBox Text="{Binding SelectedStock.Quantity, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <TextBlock Text="Location" Margin="0,8,0,4"/>
                <TextBox Text="{Binding SelectedStock.Location, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>
        </Grid>

        <!-- CRUD buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Gehitu" Command="{Binding AddCommand}" Margin="5" Width="80"/>
            <Button Content="Aldatu" Command="{Binding UpdateCommand}" Margin="5" Width="80"/>
            <Button Content="Ezabatu" Command="{Binding DeleteCommand}" Margin="5" Width="80"/>
        </StackPanel>
    </Grid>
</UserControl>