<Window x:Class="Erronka.Views.LoginView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:vm="clr-namespace:Erronka.ViewModels"
        Title="Erronka - Saioa hasi"
        Height="400" Width="460"
        WindowStartupLocation="CenterScreen"
        Background="#EFF3F8"
        ResizeMode="NoResize">

    <Window.Resources>
        <!-- Colores y pinceles -->
        <Color x:Key="AccentColor">#0078D7</Color>
        <SolidColorBrush x:Key="AccentBrush" Color="{StaticResource AccentColor}"/>
        <SolidColorBrush x:Key="CardBackgroundBrush" Color="White"/>
        <SolidColorBrush x:Key="InputBackground" Color="#F6F8FB"/>
        <SolidColorBrush x:Key="MutedText" Color="#70757A"/>

        <!-- Estilo base para inputs -->
        <Style x:Key="InputBaseStyle" TargetType="{x:Type Control}">
            <Setter Property="Height" Value="42"/>
            <Setter Property="Padding" Value="10,6"/>
            <Setter Property="Background" Value="{StaticResource InputBackground}"/>
            <Setter Property="BorderBrush" Value="#E1E6EB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Margin" Value="0,6,0,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>

        <!-- TextBox -->
        <Style TargetType="TextBox" x:Key="InputStyle" BasedOn="{StaticResource InputBaseStyle}">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer Margin="0" x:Name="PART_ContentHost" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#5078D7" BlurRadius="8" ShadowDepth="0" Opacity="0.12"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- PasswordBox -->
        <Style TargetType="PasswordBox" x:Key="PasswordInputStyle" BasedOn="{StaticResource InputBaseStyle}">
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="PasswordBox">
                        <Border CornerRadius="6" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}">
                            <ScrollViewer x:Name="PART_ContentHost" VerticalAlignment="Center" VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsFocused" Value="True">
                                <Setter Property="BorderBrush" Value="{StaticResource AccentBrush}"/>
                                <Setter Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect Color="#5078D7" BlurRadius="8" ShadowDepth="0" Opacity="0.12"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="Padding" Value="14,0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Opacity" Value="0.92"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.995" ScaleY="0.995"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#B7C9DB"/>
                                <Setter Property="Foreground" Value="#F6FBFF"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid>
        <Grid HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Background="{StaticResource CardBackgroundBrush}" CornerRadius="12" Padding="28" Width="380">
                <Border.Effect>
                    <DropShadowEffect Color="#5078D7" BlurRadius="12" ShadowDepth="0" Opacity="0.12"/>
                </Border.Effect>

                <StackPanel>
                    <TextBlock Text="GastroERP" FontSize="20" FontWeight="Bold" Foreground="#222" HorizontalAlignment="Center"/>
                    <TextBlock Text="Sartu zure kontuan" FontSize="12" Foreground="{StaticResource MutedText}" Margin="0,4,0,12" HorizontalAlignment="Center"/>

                    <StackPanel Margin="0,6,0,0">
                        <TextBlock Text="Erabiltzaile izena" FontSize="12" Foreground="{StaticResource MutedText}" Margin="2,0"/>
                        <TextBox Style="{StaticResource InputStyle}" Text="{Binding Username, UpdateSourceTrigger=PropertyChanged}" Height="42"/>
                    </StackPanel>

                    <StackPanel Margin="0,10,0,0">
                        <TextBlock Text="Pasahitza" FontSize="12" Foreground="{StaticResource MutedText}" Margin="1,0"/>
                        <PasswordBox x:Name="PasswordBox" Style="{StaticResource PasswordInputStyle}" PasswordChanged="PasswordBox_PasswordChanged" Height="42"/>
                    </StackPanel>

                    <TextBlock Text="{Binding ErrorMessage}" Foreground="Crimson" Margin="0,6,0,0" FontSize="12" HorizontalAlignment="Center"/>

                    <Button Content="Sartu" Command="{Binding LoginCommand}" Style="{StaticResource PrimaryButton}" Margin="0,14,0,0" Width="140"/>

                </StackPanel>
            </Border>
        </Grid>
    </Grid>
</Window>
