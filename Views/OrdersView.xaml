<UserControl x:Class="Erronka.Views.OrdersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:vm="clr-namespace:Erronka.ViewModels">
    <!-- DataContext removed from XAML; set in code-behind -->

    <Grid Margin="15">
        <Grid.RowDefinitions>
            <RowDefinition Height="2*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <DataGrid ItemsSource="{Binding Products}" SelectedItem="{Binding SelectedProduct}"
                  AutoGenerateColumns="False" Grid.Row="0">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Izena" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Prezioa (&#x20AC;)" Binding="{Binding Price}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
            <Button Content="Gehitu eskaerara" Command="{Binding AddToOrderCommand}" Margin="5"/>
            <Button Content="Ordaindu eta Ticket erakutsi" Command="{Binding PayCommand}" Margin="5"/>
        </StackPanel>
    </Grid>
</UserControl>