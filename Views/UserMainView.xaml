<Window x:Class="Erronka.Views.UserMainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Erabiltzailearen panela" Height="600" Width="900"
        WindowStartupLocation="CenterScreen">

    <!-- DataContext moved to code-behind to avoid designer instantiation issues -->

    <DockPanel>
        <StackPanel DockPanel.Dock="Left" Width="180" Background="#2E3A59" Margin="10">
            <TextBlock Text="Erabiltzaile menua" Foreground="White" FontWeight="Bold" FontSize="16" Margin="0,0,0,20"/>
            <Button Content="Eskaerak" Command="{Binding ShowOrdersCommand}" Margin="0,5"/>
            <Button Content="Erreserbak" Command="{Binding ShowReservationsCommand}" Margin="0,5"/>
            <Button Content="Itxi saioa" Command="{Binding LogoutCommand}" Margin="0,20,0,0"/>
        </StackPanel>

        <ContentControl Content="{Binding CurrentView}"/>
    </DockPanel>
</Window>